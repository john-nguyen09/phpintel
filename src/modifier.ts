export enum ClassModifier {
    None = 0,
    Abstract = 1,
    Final = 2,
}

export enum VisibilityModifier {
    None = 0,
    Public = 1,
    Protected = 2,
    Private = 4,
}

export enum StaticModifier {
    None = 0,
    Static = 1,
}