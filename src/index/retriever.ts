import { Symbol } from "../symbol/symbol";
import { IdentifierMatchIndex } from "./identifierMatch";

export namespace Retriever {
    export async function findSymbol(name: string): Promise<Symbol | null> {


        return null;
    }
}